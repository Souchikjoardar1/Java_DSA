for (dataType item : array) {
  // code block to be executed for each element of the array
System.out.println(x);
}

Here,
dataType: is the data type of the elements in the array.
item: is a variable that will be assigned each element of the array in turn.
array: is the array that will be iterated over.